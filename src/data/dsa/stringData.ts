import basics from './string/basics';
import advance from './string/advance';

// Only keeping the top 2 subsections (basics and advance)
const stringData = {
  basics,
  advance
};

export default stringData;
