import basics from './recursion/basics';
import advance from './recursion/advance';

// Only keeping the top 2 subsections (basics and advance)
const recursionData = {
  basics,
  advance
};

export default recursionData;
