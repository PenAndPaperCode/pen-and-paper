import basics from './twoPointer/basics';
import advance from './twoPointer/advance';

// Only keeping the top 2 subsections (basics and advance)
const twoPointerData = {
  basics,
  advance
};

export default twoPointerData;
